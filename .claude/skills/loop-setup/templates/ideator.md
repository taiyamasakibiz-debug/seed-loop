# 起案ブリーフ：<用途名>

> `ideator`（起案者）が読むインスタンス設定。この用途で「案」を**どう構成し・どんなスタンスで発想するか**を定義する。
> 前提・強み・制約・除外は `config/founder-profile.md` 側に書く（ここは「案の作り方」だけ。ダブらせない）。
> 出力フォーマットは `config/gates/` と `config/axes/` が問う観点を**確実にカバー**できる構成にする。

## 出力フォーマット（ideator が案に必ず含める見出し）
- **タイトル**
- **一言サマリ**
- **<この用途で核になる見出し1>**
- **<見出し2>**
- **<見出し3>**
- **各ゲートへの手当て**（`config/gates/` の各ゲートを、なぜ PASS できるか）
- **各軸への手当て**（`config/axes/` の各軸の「問い」に、80 ラインを満たす根拠）

## 起案スタンス（任意）
- <この用途固有の発想方針。例：「新規性より再解釈・異分野転用を重視」>
- <トレンド補助(WebSearch)の扱い。例：素材として使い、前提を逸脱しない>
